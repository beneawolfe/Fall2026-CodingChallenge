// TEMPORARY placeholder for image search (built in Step 6).

import { Typography } from '@mui/material';

export default function SearchPage() {
  return (
    <>
      <Typography variant="h4" gutterBottom>
        Search
      </Typography>
      <Typography color="text.secondary">Image search is coming in a later step.</Typography>
    </>
  );
}